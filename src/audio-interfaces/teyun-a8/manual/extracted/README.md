# Searchable Manual Text

No original A8 manual PDF is stored yet, so there is no local text extraction.
When a printed or official PDF source is available, retain it in `../source/`,
record its checksum in `../manuals.json`, and generate UTF-8 text here with
`pdftotext -layout`.
