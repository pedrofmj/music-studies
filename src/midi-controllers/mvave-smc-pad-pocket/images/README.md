# Images

Store photographs and screenshots that prove a hardware state or configuration:
rear-panel wiring, power and battery indicators, DAW MIDI settings, editor
preset pages, firmware screens, and pad or controller layouts. Use filenames
that begin with `YYYY-MM-DD` and add a corresponding mapping or backup-log
entry.
