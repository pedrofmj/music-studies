# Official Manual PDF Cache

This directory contains the original official manual PDF downloaded for internal
study. The parent `manuals.json` file records its official page, direct download
URL, page count, byte size, and SHA-256 digest.

Do not replace the PDF without updating the parent manifest and validating the
new digest.
