# Searchable Carla Manual Text

No extracted text exists because no local PDF is retained. When an authorized
source PDF is added, run pdftotext -layout and store the matching text file
here without manual edits.

The source PDF remains authoritative for diagrams, layout, and page numbering.
